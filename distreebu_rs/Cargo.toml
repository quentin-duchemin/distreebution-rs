[package]
name = "distreebu_rs"
version = "0.2.0"
edition = "2021"

[lib]
name = "distreebu_rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
rayon = "1.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
